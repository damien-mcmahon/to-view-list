const initialTVShowState = [
  {
    id: 12345,
    name: "Test"
  }
];

export default function tvShows(state = initialTVShowState, action) {
  switch(action.type) {
    default: 
      return state;
  }
}
